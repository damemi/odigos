{{- if and .Values.interrogation.enabled (include "odigos.secretExists" .) }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: odigos-interrogation
  namespace: {{ .Release.Namespace }}
  labels:
    odigos.io/system-object: "true"
{{- end }}
