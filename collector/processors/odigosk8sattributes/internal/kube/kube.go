package kube

// Pod represents a Kubernetes pod with its associated metadata
type Pod struct {
	// Add fields as needed for pod information
	// This is a placeholder struct that can be extended with pod-specific fields
}
