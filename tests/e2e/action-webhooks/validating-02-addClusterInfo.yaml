apiVersion: odigos.io/v1alpha1
kind: Action
metadata:
  name: 02-action-addclusterinfo
  namespace: default
spec:
  signals:
  - TRACES
  config:
    clusterAttributes:
    - attributeName: foo
      attributeStringValue: bar
